Claude Code reads `statusLine` when it starts, so **restart Claude Code** for
the bridge to take effect.

Once it renders its status line once, a reading appears in the VS Code status
bar for any window whose folder matches a running session.

Nothing to click here — this step is just the wait.
