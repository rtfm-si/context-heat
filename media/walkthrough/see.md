You cannot reach 90% context on demand, so **Simulate Temperature** turns the
dial by hand.

Try 80%. The status bar should read `🔥🔥🔥 80% COMPACT` and your editor chrome
should heat up — faintest at the title bar, strongest at the status bar.

**Clear Simulation** returns to the real number.
